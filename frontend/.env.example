# このファイルを .env にコピーして値を設定する (Viteは VITE_ 接頭辞の変数のみ露出)。
# フロントはここで指定した backend 1か所だけを見る。

# ローカル開発: バックエンドは localhost:8787。
# 本番(Vercel): Render のバックエンドURL (例 https://hybrid-macro-desk-api.onrender.com) を設定する。
VITE_API_BASE=http://localhost:8787
