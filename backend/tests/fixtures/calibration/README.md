# 校正場面の記録済み実データ

`python -m setup_console.record_calibration` (Mac で実行) が `S1.json` `S2.json` `S3.json` `NEG_0828.json` を生成する。
サンドボックスからは Binance に到達できないため、ここに置かれるまで実データ依存のテストは skip される。
公開 klines のみ (キー不要)。
